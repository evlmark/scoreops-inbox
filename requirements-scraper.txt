# Зависимости сервиса-скрапера (отдельный Railway-сервис).
# Web-сервис их НЕ тянет — у него requirements.txt без playwright/bs4.
playwright==1.44.0
beautifulsoup4==4.12.3
lxml==5.2.2
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
httpx==0.27.2
python-dotenv==1.0.1
